# 2026-04-26 Step3.6 LLM 记忆提取器 Worklog

## 本次完成

- 在 `memory_extractor.py` 中加入 LLM JSON 记忆提取器。
- 支持输出 `fact`、`relationship`、`preference`、`constraint`、`long_term_goal`、`mood_signal`、`rhythm_signal`、`care_preference`。
- 对 LLM 输出做 JSON 解析、类型白名单、敏感等级校验、置信度/重要性裁剪。
- LLM 失败或没有提取结果时自动回退到规则型提取，不影响主私聊回复。
- `/api/v1/jarvis/chat` 已把现有 `llm_client` 传入记忆提取器，形成真实链路。
- 更新 `4-26plan.md`，Step 3.6 进度改为 LLM JSON + 规则兜底记忆提取闭环。
- 新增文档：`worklog-lsq/2026-04-26_Jarvis长期记忆查看与删除说明.md`。

## 验证

命令：

```bash
python -m pytest -p no:langsmith tests\unit\jarvis\test_memory_extractor.py
```

结果：4 passed。

## 下一步

- 接前端记忆管理页，展示 `memory_kind`、`sensitivity`、`confidence`、`importance`，并支持删除。
- 增加情绪/节律趋势聚合接口，让 Mira/Nora 能做低打扰关心。
- 做向量或关键词相关性召回，避免每次只按重要性注入。
