# 2026-04-26 Step3.6 记忆提取层最小闭环 Worklog

## 本次判断

阅读 `4-26plan.md` 后，当前优先级应先做 Step 3.6。原因：Step 1 已打通日程确认写入，Step 3.5 已有后台任务拆解与确认保存；Step 3.6 是唯一仍处于“规划完成 / 未正式实现”的关键基础层，并且它直接支撑后续心理/节律记忆、路演可追溯和长期个性化。

## 本次完成

- 在 `shadowlink-ai/app/jarvis/persistence.py` 新增 Demo 记忆与追踪表：`demo_runs`、`demo_trace_events`、`demo_memory_items`。
- 新增 Demo 数据重置、开始 Demo Run、记录 Demo Trace、保存 Demo Memory、导出 Demo Trace 的持久层函数。
- 在 `shadowlink-ai/app/api/v1/jarvis_router.py` 新增 `/api/v1/jarvis/demo/*` API：
  - `POST /demo/reset`
  - `POST /demo/runs`
  - `GET /demo/runs`
  - `POST /demo/runs/{demo_run_id}/trace`
  - `POST /demo/runs/{demo_run_id}/memories`
  - `GET /demo/runs/{demo_run_id}/trace`
- 在 `shadowlink-ai/tests/unit/jarvis/test_persistence.py` 补充 Demo Run / Demo Trace / Demo Memory 的单元测试用例。
- 更新 `4-26plan.md`：Step 3.6 从“规划完成 / 雏形存在”调整为“部分实现”，并记录剩余缺口。

## 验证情况

- 尝试运行 `python -m pytest tests\unit\jarvis\test_persistence.py`。
- 当前系统 Python 环境缺少可用的 `pytest-asyncio` 插件，直接 pytest 会导致 async 测试无法执行；同时全局 pytest 插件曾触发 `D:\sslkey.log` 权限问题。
- 已用独立 smoke 脚本验证 Demo 持久层主链路：创建 Demo Run、写入 Demo Memory、追加 Demo Trace、导出 Trace、重置 Demo 数据，结果通过。

## 下一步建议

1. 把 Demo Memory API 接到路演脚本，让每个 demo_step_id 都能产出可导出的 trace。
2. 做主对话结束后的轻量记忆提取器，把明确偏好、事实、心理/节律信号写入统一候选记忆。
3. 增加前端 Demo Trace 查看页或最小 JSON 导出按钮。
4. 修复/补齐本机测试环境中的 `pytest-asyncio`，恢复标准 pytest 验证。
