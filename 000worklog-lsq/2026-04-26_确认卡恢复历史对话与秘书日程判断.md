# 2026-04-26 确认卡恢复、历史对话与秘书日程判断修复

## 本次目标

先修复两个真实产品阻断点，再继续推进 Step 3 的秘书日程判断：

1. 待确认卡片在前端看不到/刷新后消失。
2. 刷新或点击返回后，私聊记录没有稳定进入历史对话。
3. Maxwell 安排日程前需要有可见的判断层：冲突、过去时间、日程密度、替代空档。

## 已完成

- 后端 `agent_chat_turns` 增加 `actions` 字段，并在初始化时做兼容迁移。
- Agent 回复落盘时保存 `action_results`，聊天历史接口回传 actions。
- 前端 `loadChatHistory` 恢复 actions，刷新/返回后仍能渲染待确认卡。
- 私聊页点击“返回”时主动调用 `saveConversationHistory`，记录进入历史对话模块。
- `jarvis_calendar_add` 待确认卡生成前加入 `schedule_guard`：
  - 检查开始时间是否已过去。
  - 检查与未来日程是否重叠。
  - 根据附近日程压力做密度提醒。
  - 计算最多 3 个替代空档。
- 前端待确认日程卡展示秘书判断、冲突列表和替代空档。
- `4-26plan.md` 已同步 Step 3 / Step 3.5 的新增完成内容。

## 验证

- 后端语法：`python -m py_compile app\jarvis\persistence.py app\api\v1\jarvis_router.py app\jarvis\tool_runtime.py`
- 前端类型：`npm.cmd run type-check`
- 两项均通过。

## 下一步建议

继续 Step 3：让用户能在待确认日程卡里直接选择秘书给出的替代空档，点击后用替代时间写入日程，而不是只能看建议再手动重说。
